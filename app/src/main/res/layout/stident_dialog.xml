<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingLeft="@dimen/activity_margin"
    android:paddingRight="@dimen/activity_margin"
    android:paddingTop="@dimen/activity_margin">

    <!-- Title of dialog -->
    <TextView
        android:id="@+id/dialog_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/dimen_10"
        android:fontFamily="sans-serif-medium"
        android:lineSpacingExtra="8sp"
        android:text="@string/lbl_new_note_title"
        android:textColor="#FB3838"
        android:textSize="@dimen/lbl_new_note_title"
        android:textStyle="normal" />

    <!-- Edit Text for student name -->
    <EditText
        android:id="@+id/studentInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:gravity="top"
        android:hint="@string/hint_enter_note"
        android:inputType="textCapSentences|textMultiLine"
        android:lines="4"
        android:textSize="@dimen/input_new_note"
        android:importantForAutofill="no" />

    <!-- Edit text for course information -->
    <EditText
        android:id="@+id/classInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:gravity="top"
        android:hint="@string/hint_enter_note2"
        android:inputType="textCapSentences|textMultiLine"
        android:lines="4"
        android:textSize="@dimen/input_new_note"
        android:importantForAutofill="no" />

    <!-- edit text for entries priority level -->
    <EditText
        android:id="@+id/priorityEdit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="@string/_1_5_freshman_to_graduate"
        android:inputType="number"
        android:importantForAutofill="no" />

</LinearLayout>